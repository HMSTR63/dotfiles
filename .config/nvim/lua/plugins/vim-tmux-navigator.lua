return {
  "christoomey/vim-tmux-navigator",
  lazy = false,
  config = function()
    -- This plugin does not require any specific Neovim configuration for basic functionality.
    -- It works out of the box with the corresponding tmux configuration.
  end,
}
